import SectionContainer from "@/ui/SectionContainer";

const Hero = () => {
  return (
    <SectionContainer
      sectionName="hero"
      sectionClasses="h-[75vh] bg-fixed bg-cover bg-center bg-no-repeat"
    >
      <div className="hidden">Test</div>
    </SectionContainer>
  );
};

export default Hero;
